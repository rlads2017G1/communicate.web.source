setwd("C:/Users/user/rlads_communicate/communicate.web.source/china")
library(DT)
library(readr)
rankingkable <- read_csv("./rankingkable.csv")
rankingkable <- as.data.frame(rankingkable)
View(rankingkable)
sketch = htmltools::withTags(table(
tableHeader(rankingkable),
tableFooter(rankingkable)
))
## Highlight row in Table
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 10, dom = 'tip')) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffa616'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
)
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip')) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffa616'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
)
table <- datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip')) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
table <- datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip')) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
table <- datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center'))) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
rm(list=ls())
library(DT)
library(readr)
rankingkable <- read_csv("./rankingkable.csv")
rankingkable <- as.data.frame(rankingkable)
# a custom table with both header and footer
sketch = htmltools::withTags(table(
tableHeader(rankingkable),
tableFooter(rankingkable)
))
## Highlight row in Table
table <- datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center'))) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
table
View(table)
table
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center'))) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
sketch = htmltools::withTags(table(
tableHeader(rankingkable),
tableFooter(rankingkable)
))
## Highlight row in Table
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center'))) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center') )) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030')))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
datatable(iris, rownames = FALSE, container = sketch,
options = list(pageLength = 5, dom = 'tip')) %>%
formatStyle('Petal.Length', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual(c(1.4, 1.7), c('gray', 'yellow'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
)
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center',targets="_all") )) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
)
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center',targets="_all") )) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip' )) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
)
??columnDefs
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center',targets="_all"))) %>%
formatStyle('Petal.Length', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual(c(1.4, 1.7), c('gray', 'yellow'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
)
## Highlight row in Table
datatable(rankingkable, rownames = FALSE,
options = list(pageLength = 15, dom = 'tip', columnDefs=list(className = 'dt-center',targets="_all"))) %>%
formatStyle('Petal.Length', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual(c(1.4, 1.7), c('gray', 'yellow'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
)
library(DT)
library(readr)
rankingkable <- read_csv("./rankingkable.csv")
rankingkable <- as.data.frame(rankingkable)
# a custom table with both header and footer
sketch = htmltools::withTags(table(
tableHeader(rankingkable),
tableFooter(rankingkable)
))
## Highlight row in Table
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(
pageLength = 15,
dom = 'tip',
columnDefs = list(list(className = 'dt-center', targets = 0:4))
)) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
debugSource('C:/Users/user/rlads_communicate/communicate.web.source/china/plot_fc.R', encoding = 'UTF-8')
debugSource('C:/Users/user/rlads_communicate/communicate.web.source/china/plot_fc.R', encoding = 'UTF-8')
debugSource('C:/Users/user/rlads_communicate/communicate.web.source/china/plot_fc.R', encoding = 'UTF-8')
debugSource('C:/Users/user/rlads_communicate/communicate.web.source/china/plot_fc.R', encoding = 'UTF-8')
rm(list = ls())
debugSource('C:/Users/user/rlads_communicate/communicate.web.source/china/plot_fc.R', encoding = 'UTF-8')
# a custom table with both header and footer
sketch = htmltools::withTags(table(
tableHeader(rankingkable),
tableFooter(rankingkable)
))
## Highlight row in Table
datatable(rankingkable, rownames = FALSE, container = sketch,
options = list(
pageLength = 15,
dom = 'tip',
columnDefs = list(list(className = 'dt-center',targets="_all"))
)) %>%
formatStyle('8', ## select target column to compare
target = 'row',  ## highlight row
backgroundColor = styleEqual("韓國",'#ffb030'))
## gray: for rows with 1.4 at Petal.Length
## yellow: for rows with 1.7 at Petal.Length
